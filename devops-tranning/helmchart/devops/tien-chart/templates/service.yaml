apiVersion: v1
kind: Service
metadata:
  name: {{ include "tien-chart.fullname" . }}
  labels:
    {{- include "tien-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tien-chart.selectorLabels" . | nindent 4 }}
